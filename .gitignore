# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
# backend has separate package-lock and separate node_modules that need to be filtered out
/src/backend/node_modules
# config file contains the local IP of the express backend, you can change the template, but it is ignored so our changes do not all get committed each time
/src/config.json
# backend log files should not be committed
/src/backend/logs
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
